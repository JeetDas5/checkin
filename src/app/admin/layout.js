export const metadata = {
  title: "Admin Panel | Attendance Tracker",
  description: "Administrative panel for managing users and roles.",
};

export default function AdminLayout({ children }) {
  return <>{children}</>;
}
